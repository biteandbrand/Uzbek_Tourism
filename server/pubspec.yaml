name: uztour_server
description: Uzbek Tour backend API (Dart + shelf), docs/API.md sözleşmesini sunar.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  postgres: ^3.5.0
  http: ^1.2.2
  minio: ^3.5.0            # S3 uyumlu — Cloudflare R2 için

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
